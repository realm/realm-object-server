{
    "name": "my-ros-app",
    "version": "1.0.0",
    "description": "My new ROS App",
    "main": "src/index.js",
    "scripts": {
        "build": "rm -rf dist; ./node_modules/.bin/tsc",
        "clean": "rm -rf dist",
        "start": "npm run build && node dist/index.js"
    },
    "devDependencies": {
        "typescript": "2.5.3"
    },
    "dependencies": {
        "realm-object-server": "^3.12.1"
    }
}
